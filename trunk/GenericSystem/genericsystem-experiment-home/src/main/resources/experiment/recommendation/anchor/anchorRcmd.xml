<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:jdbc="http://www.springframework.org/schema/jdbc"
	xmlns:util="http://www.springframework.org/schema/util"
	xsi:schemaLocation="http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc-3.1.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/data/jpa http://www.springframework.org/schema/data/jpa/spring-jpa-1.2.xsd
		http://www.springframework.org/schema/util
		http://www.springframework.org/schema/util/spring-util-2.5.xsd">

	<!-- ******************************************** Engine Setting************************************************ -->
	<bean id="anchorRcmd" class="edu.tongji.engine.recommendation.LLRAMixtureRcmdEngine">
		<property name="trainingSetFile" value="${TRAININGSET_READ_PATH}" />
		<property name="testingSetFile" value="${TESTINGSET_READ_PATH}" />
		<property name="userCount" value="${USER_COUNT}" />
		<property name="itemCount" value="${ITEM_COUNT}" />

		<property name="maxValue" value="${MAX_RATING}" />
		<property name="minValue" value="${MIN_RATING}" />
		<property name="featureCount" value="${FEATURE_COUNT}" />
		<property name="learningRate" value="${LEARNING_RATE}" />
		<property name="regularizer" value="${REGULARIZED_PARAM}" />
		<property name="momentum" value="${MOMENTUM}" />
		<property name="maxIter" value="${MAX_ITERATION}" />
		<property name="base1" value="${B1}" />
		<property name="base2" value="${B2}" />
		<property name="modelMax" value="${MODELMAX}" />
		<property name="parser" ref="movielensRatingTemplateParser" />
	</bean>

	<!-- ******************************************** Thread Setting************************************************ -->
	<bean id="movielensRatingTemplateParser" class="edu.tongji.parser.MovielensRatingTemplateParser" />


	<!-- **************************** Outter Setting Connector ************************************************ -->
	<bean id="propertyConfigurer"
		class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="location">
			<value>
				classpath:experiment/recommendation/anchor/z_anchor.properties
			</value>
		</property>
	</bean>

</beans>
